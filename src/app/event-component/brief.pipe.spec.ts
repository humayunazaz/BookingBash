/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { BriefPipe } from './brief.pipe';

describe('BriefPipe', () => {
  it('create an instance', () => {
    let pipe = new BriefPipe();
    expect(pipe).toBeTruthy();
  });
});
