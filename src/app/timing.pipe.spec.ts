/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { TimingPipe } from './timing.pipe';

describe('TimingPipe', () => {
  it('create an instance', () => {
    let pipe = new TimingPipe();
    expect(pipe).toBeTruthy();
  });
});
